# Test-only dependencies for ai-python. Kept separate from requirements.txt
# so the production image doesn't ship pytest. Install with:
#     pip install -r requirements-dev.txt
-r requirements.txt

pytest==8.3.4
pytest-asyncio==1.4.0     # only needed if we ever test async endpoints
httpx==0.28.1              # used by FastAPI's TestClient under the hood
